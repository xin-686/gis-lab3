# 实验三 WebGIS 二维开发与个人GIS作品集（P1）—— 实验报告填写内容

> 使用方法：将下方每一部分内容复制粘贴到你的 Word 实验报告对应栏目中，并按照"建议截图"插入图片。

---

## 实验目标

本实验围绕 WebGIS 二维开发展开，基于 Leaflet 1.9.4 和 MapLibre GL JS 5.6.2，完成以下五个核心内容：

1. Leaflet 地图与标注交互——多底图切换、POI 标注、地图书签、距离测量；
2. GeoJSON 专题地图——分级设色图与比例符号图制作及图例设计；
3. MapLibre GL JS 矢量瓦片可视化——数据驱动样式、点聚合、样式切换与建筑 3D 拉伸；
4. 二维综合作品集（P1）——"城市学习足迹与兴趣点档案"综合 GIS 作品集；
5. 部署说明与拓展——本地运行指南、GitHub Pages 部署步骤及两项加分功能（双变量专题图、标注编辑器）。

---

## （1）Leaflet 地图与标注交互（15分）

### 实现内容

- 初始化 Leaflet 地图，使用 `L.map` 创建地图实例，默认加载 OpenStreetMap 在线瓦片底图。
- 通过 `L.control.layers` 实现多底图切换，提供 OSM 标准、Carto 浅色、Carto 深色、ESRI 卫星影像共 4 种底图方案。天地图选项保留在代码中（需填写个人 key）。
- 添加 10 个多类型 POI 标注点（校园学习点、文化场馆、自然景观、交通枢纽），使用 `L.divIcon` 自定义图标（圆形彩色图标 + 中文标识字），每个 POI 绑定 `bindTooltip`（名称+城市）和 `bindPopup`（名称、城市、类型、重要度、签到次数、说明）。
- 实现 6 个地图书签（全国总览、上海学习圈、武汉研学点、粤港澳观察、北京文化圈、成渝走廊），点击后 `flyTo` 动画定位。
- 添加比例尺（`L.control.scale`）、实时鼠标坐标显示（`mousemove` 事件更新）、距离测量工具（单击连续取点→双击结束，实时显示累计距离）。

### 核心代码（可粘贴到报告）

```javascript
// 多底图定义
var layers = {
  osm: L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"),
  cartoLight: L.tileLayer("https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png"),
  cartoDark: L.tileLayer("https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png"),
  esriWorld: L.tileLayer("https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}")
};
L.control.layers(baseMaps, null, { position: "topright" }).addTo(leafletMap);

// 自定义 POI 图标
function createPoiIcon(cat) {
  return L.divIcon({
    className: "",
    html: '<div class="poi-marker ' + cat + '">' + getPoiEmoji(cat) + '</div>',
    iconSize: [30, 30], iconAnchor: [15, 15], popupAnchor: [0, -18]
  });
}

// 距离测量（单击取点，双击结束）
leafletMap.on("click", function(e) {
  if (!measuring) return;
  measurePoints.push(e.latlng);
  // ... 重绘折线，更新状态
});
leafletMap.on("dblclick", function() {
  measuring = false;
  statusEl.textContent = "测量完成，总距离 " + formatDistance(getTotal());
});
```

### 建议截图
1. **Leaflet 全景图**：OSM 底图 + 所有 POI 标注可见 + 右上角图层控件展开
2. **底图切换对比**：分别切换到 Carto 深色和 ESRI 卫星影像的截图（两张）
3. **地图书签效果**：点击"武汉研学点"书签后地图定位到武汉的截图
4. **距离测量演示**：在地图上画出一条测量线并显示距离的截图

---

## （2）GeoJSON 专题地图（30分）

### 实现内容

- 加载中国省级行政区 GeoJSON（`china-provinces.geojson`，34 个省级单位），使用 `fetch` + `L.geoJSON` 加载。
- 通过 `onEachFeature` 绑定 mouseover/mouseout 高亮交互和 Popup 信息展示。
- 制作**分级设色图**：`getChoroplethColor` 函数按 studyIndex 分为 5 级（80-84-88-92+），颜色从浅蓝到深蓝，使用 `style` 回调设置 `fillColor`。
- 制作**比例符号图**：在行政区中心点使用 `L.circleMarker`，半径按 visitCount 映射（`radius = visitCount × 1.4`），展示调研访问次数。
- 使用 `pointToLayer` 渲染点要素（圆点标记），`filter` 过滤 `importance >= 4` 的 POI。
- 配色方案：蓝色顺序色带（Blue Sequential），从 `#dbeafe`（浅蓝）到 `#0b3c8a`（深蓝），适合表达从低到高的连续数量变化。

### 核心代码（可粘贴到报告）

```javascript
// 分级设色函数
function getChoroplethColor(value) {
  if (value >= 92) return "#0b3c8a";
  if (value >= 88) return "#2563eb";
  if (value >= 84) return "#60a5fa";
  if (value >= 80) return "#93c5fd";
  return "#dbeafe";
}

// 比例符号半径
function getSymbolRadius(value) {
  return Math.max(8, value * 1.4);
}

// 分级设色图 + 高亮交互
var geoLayer = L.geoJSON(regions, {
  style: function(f) {
    return { fillColor: getChoroplethColor(f.properties.studyIndex), fillOpacity: 0.78 };
  },
  onEachFeature: function(f, layer) {
    layer.on({
      mouseover: function(e) { e.target.setStyle({ weight: 3, fillOpacity: 0.9 }); },
      mouseout: function(e) { geoLayer.resetStyle(e.target); }
    });
  }
});

// pointToLayer + filter 过滤
L.geoJSON(PORTFOLIO_POI, {
  filter: function(f) { return f.properties.importance >= 4; },
  pointToLayer: function(f, latlng) {
    return L.circleMarker(latlng, { radius: 6 + f.properties.importance });
  }
});
```

### 建议截图
1. **分级设色图全景**：8 个省级行政区按颜色深浅显示 + 左下角图例
2. **比例符号图**：行政区中心圆形大小对比 + 右下角图例
3. **高亮交互**：鼠标悬停在某个省上时边框加粗高亮的效果
4. **点要素过滤**：importance >= 4 的 POI 展示在专题图上的截图

---

## （3）MapLibre GL JS 矢量瓦片可视化（25分）

### 实现内容

- 创建 MapLibre 地图实例（`new maplibregl.Map`），使用本地 Style JSON 对象作为底图（包含 background 和 graticule 图层），并在 style.load 时动态添加覆盖图层。
- 添加 GeoJSON Source（focus-regions、portfolio-pois、portfolio-route、portfolio-buildings），构建 Fill/Line/Circle/fill-extrusion 等多类型 Layer。
- 实现**数据驱动样式**：点颜色按 `importance` 属性分级（`interpolate` 表达式，3→低/4→中/5→高），点半径按 `checkins` 映射。
- 实现**样式切换器**：3 套底图配色（Liberty 浅色/Positron 灰白/Dark 深色）× 3 套数据主题（冷色/暖色/对比）= 9 种组合。
- 实现**点聚合可视化**：`cluster: true, clusterRadius: 46`，聚合圆半径按 `point_count` 分级，点击聚合圆展开到子级。
- 使用 **fill-extrusion** 实现建筑 3D 拉伸：6 栋模拟建筑（同济大学周边），高度 45-120m，通过 `fill-extrusion-height` 和 `fill-extrusion-base` 控制。

### 核心代码（可粘贴到报告）

```javascript
// 点聚合 Source
maplibreMap.addSource("ml-poi-src", {
  type: "geojson", data: PORTFOLIO_POI,
  cluster: true, clusterRadius: 46, clusterMaxZoom: 12
});

// 聚合圆 Layer
maplibreMap.addLayer({
  id: "ml-clusters", type: "circle", source: "ml-poi-src",
  filter: ["has", "point_count"],
  paint: {
    "circle-radius": ["step", ["get", "point_count"], 18, 5, 24, 9, 30],
    "circle-color": ["step", ["get", "point_count"], "#2563eb", 5, "#0ea5e9", 9, "#111827"]
  }
});

// 数据驱动样式（颜色按 importance 分级）
maplibreMap.addLayer({
  id: "ml-points", type: "circle", source: "ml-poi-src",
  filter: ["!", ["has", "point_count"]],
  paint: {
    "circle-color": ["interpolate", ["linear"], ["get", "importance"],
      3, "#60a5fa", 4, "#2563eb", 5, "#1d4ed8"],
    "circle-radius": ["interpolate", ["linear"], ["get", "checkins"], 6, 8, 16, 18]
  }
});

// 建筑 3D 拉伸
maplibreMap.addLayer({
  id: "ml-buildings", type: "fill-extrusion", source: "ml-build-src",
  minzoom: 10,
  paint: {
    "fill-extrusion-height": ["coalesce", ["get", "height"], 12],
    "fill-extrusion-base": ["coalesce", ["get", "min_height"], 0],
    "fill-extrusion-opacity": 0.74
  }
});
```

### 建议截图
1. **MapLibre 全景**：Liberty 浅色底图 + 聚合圆 + 省份专题 + 路线可见
2. **样式切换对比**：Dark 深色底图截图 + Positron 灰白底图截图
3. **数据主题切换**：暖色主题和对比主题各一张
4. **点击聚合展开**：点击一个聚合圆后放大到子点的截图
5. **3D 建筑**：zoom >= 10 时在上海区域看到彩色 3D 建筑拉伸的截图

---

## （4）二维综合作品集 P1（15分）

### 实现内容

- 作品名称为"**城市学习足迹与兴趣点档案**"，围绕个人跨区域学习足迹与城市兴趣点展开。
- 图层结构（4 层）：
  - 基础底图图层：OSM 标准瓦片
  - 区域专题图层：8 个省级行政区按 studyIndex 分级设色
  - 个人足迹路线图层：按 sequence 序号串联 10 个 POI 的 LineString
  - 兴趣点标注图层：10 个 POI 自定义图标
- 交互设计：右上角图层控件（可开关各覆盖层），点击省份/POI 触发侧边栏信息面板联动。
- 界面风格统一：深蓝渐变 Hero 区域 + 白色卡片面板 + 统一圆角和阴影。

### 作品集功能说明（可直接粘贴到报告）

> 本作品集以"城市学习足迹与兴趣点档案"为主题，整合了 Leaflet 地图引擎的多个核心能力，形成一张综合 GIS 作品集地图。
>
> **数据内容**：选取北京、上海、江苏、浙江、安徽、湖北、广东、四川共 8 个省级行政区作为专题分析区域，每个区域标注了学习关注指数（studyIndex）、调研访问次数（visitCount）、创新评价分数和主题标签。同时在地图上标注了 10 个个人足迹 POI 点，涵盖校园学习点、文化场馆、自然景观和交通枢纽四种类型。
>
> **专题表达**：分级设色图层用蓝色顺序色带表达学习关注指数从低到高的空间分布；足迹路线图层用绿色虚线按时间顺序串联各 POI，形成清晰的空间轨迹。
>
> **交互方式**：右上角图层控件支持独立开关三个覆盖层；点击省份或 POI 触发起信息面板更新；鼠标悬停 POI 显示 Tooltip，点击显示完整 Popup。

### 建议截图
1. **P1 全景图**：三种覆盖层全部打开 + 图层控件展开
2. **信息面板联动**：点击某个省份后侧边栏显示该省详情的截图
3. **单图层效果**：只开启"足迹路线"图层的截图

---

## （5）部署上线与拓展（15分，加分项）

### 部署内容

**本地运行方式：**
```bash
cd D:\GIS实验三
python -m http.server 8000
# 访问 http://localhost:8000/
```

**PBF 矢量瓦片模式**（Planetiler 转换完成后）：
```bash
# 终端1：启动瓦片服务（端口 8080）
npx tileserver-gl-light tiles-data\china.mbtiles -p 8080
# 终端2：启动页面服务（端口 8000）
python -m http.server 8000
```

**GitHub Pages 部署步骤：**
1. 新建 GitHub 仓库，上传项目文件（index.html, app.js, styles.css, data/ 目录）
2. 进入仓库 Settings → Pages
3. Source 选择 Deploy from a branch，Branch 选择 main，文件夹选择 / (root)
4. 保存后等待 1-2 分钟，获得公开访问链接
5. 填入报告：`https://你的用户名.github.io/仓库名/`

**在线访问链接：**（请发布后填写）____________________

### 拓展功能 1：双变量专题图（+7分）

同时展示 studyIndex 和 visitCount 两个变量，使用 3×3 双变量色带矩阵：
- 横轴：学习关注指数（低/中/高）
- 纵轴：访问次数（低/中/高）
- 左下角浅灰色 = 低关注 + 低访问，右上角深蓝绿色 = 高关注 + 高访问
- 颜色从对角线上行表示两变量同步递增

### 拓展功能 2：标注编辑器（+7分）

- 进入添加模式后，点击地图任意位置添加自定义标注点
- 支持编辑名称、类别（校园/文化场馆/景观/交通枢纽）、备注
- 支持删除单个标注、清空全部标注
- 一键导出自定义标注为 GeoJSON 文件

### 建议截图
1. **部署说明页**：展示"本地运行"和"GitHub Pages"面板
2. **双变量专题图**：3×3 矩阵 + 行政区按双变量颜色显示
3. **标注编辑器**：已在地图上添加多个标注点 + 标注详情面板

---

## 图层结构说明（可直接粘贴到报告）

| 图层名称 | 类型 | 数据来源 | 渲染方式 | 交互方式 |
|---------|------|---------|---------|---------|
| OSM 底图 | 栅格瓦片 | tile.openstreetmap.org | L.tileLayer | 图层控件切换 |
| Carto 浅色底图 | 栅格瓦片 | basemaps.cartocdn.com | L.tileLayer | 图层控件切换 |
| ESRI 卫星影像 | 栅格瓦片 | server.arcgisonline.com | L.tileLayer | 图层控件切换 |
| POI 标注图层 | 矢量点 | 内置 GeoJSON | L.geoJSON + divIcon | Tooltip + Popup |
| 分级设色图层 | 矢量面 | china-provinces.geojson | L.geoJSON + style回调 | mouseover 高亮 + Popup |
| 比例符号图层 | 矢量点 | 行政区中心坐标 | L.circleMarker | Tooltip |
| 过滤点图层 | 矢量点 | 内置 GeoJSON | L.circleMarker (filter) | Tooltip |
| 足迹路线图层 | 矢量线 | 内置 GeoJSON | L.geoJSON LineString | 鼠标悬停样式变化 |
| 聚合点图层 | 矢量点 | 内置 GeoJSON | MapLibre cluster | 点击展开 |
| 3D 建筑图层 | fill-extrusion | 内置 GeoJSON | MapLibre fill-extrusion | 鼠标悬停 cursor |

## 专题图配色方案说明（可直接粘贴到报告）

**选择依据**：本实验采用蓝色顺序色带（Blue Sequential Color Scheme），具体为 `#dbeafe → #93c5fd → #60a5fa → #2563eb → #0b3c8a` 共 5 级。

**理由**：
1. **语义匹配**："学习关注指数"是从低到高的单向递增变量，顺序色带能直观传达"越大越深"的视觉暗示，无需发散色带的"零值中心"语义。
2. **可读性**：蓝色系在白色底图上对比度充足，各级之间色差明显，支持 5 级有效区分。
3. **色盲友好**：蓝色色带对红绿色盲使用者也能有效辨别明度变化。

## 部署说明（可直接粘贴到报告）

本项目为纯前端静态页面，无需构建工具，可直接部署到任意静态文件服务器：

- **本地运行**：`python -m http.server 8000`，访问 `http://localhost:8000/`
- **GitHub Pages**：将项目文件上传至 GitHub 仓库，在 Settings → Pages 中启用，选择 main 分支根目录发布。
- **底图说明**：默认使用 OpenStreetMap 在线瓦片，无需额外配置即可运行。本地 PBF 矢量瓦片模式需先运行 `npx tileserver-gl-light tiles-data\china.mbtiles -p 8080` 启动本地瓦片服务。

---

## 实验总结与心得（不少于200字，可直接粘贴到报告）

本次实验通过 AI 辅助编程工具完成了从零搭建一个完整 WebGIS 实验项目，涉及 Leaflet、GeoJSON 专题图、MapLibre GL JS 矢量瓦片可视化、个人 GIS 作品集的综合开发与部署。整个过程中，我将第 08-12 讲的课后练习逐一串联到同一个项目中，形成了从基础底图加载、POI 标注、专题图制作到三维可视化的完整技术链路。

在技术实现层面，主要收获包括：深刻理解了 Leaflet 的图层管理机制和 L.geoJSON 的 style/onEachFeature/filter 回调体系；掌握了分级设色图（Choropleth）和比例符号图（Proportional Symbol）的制作方法及图例设计；学习了 MapLibre 的 Style JSON 规范、数据驱动样式表达式和 fill-extrusion 3D 建筑渲染。在交互设计方面，实现了图层控件切换、地图书签定位、距离测量、信息面板联动等用户体验功能。

遇到的主要问题：一是底图加载失败——最初的离线底图方案只是简单的色块和网格线，不具备真实的地图参考价值。解决方法是将默认底图切换为 OSM 在线瓦片，并提供了 Carto、ESRI 卫星影像等多种备选方案。二是中国行政区 GeoJSON 数据较大（约 15MB），需要合理设计数据结构和缓存策略。三是 MapLibre 的 style.load 事件与动态图层添加的时序问题，通过检查 source/layer 是否已存在来避免重复添加。

通过本次实验，我不仅掌握了 WebGIS 二维开发的核心技术栈，还体验了从本地开发到线上部署的完整工程流程，为后续三维 WebGIS 学习和个人 GIS 作品集（P2）打下了坚实基础。

---

## 截图清单汇总

| 序号 | 对应模块 | 截图内容 |
|------|---------|---------|
| 1 | (1) Leaflet | OSM 底图 + POI 标注 + 图层控件展开 |
| 2 | (1) Leaflet | Carto 深色底图切换效果 |
| 3 | (1) Leaflet | ESRI 卫星影像底图 |
| 4 | (1) Leaflet | 点击地图书签后定位效果 |
| 5 | (1) Leaflet | 距离测量折线 + 累计距离显示 |
| 6 | (2) GeoJSON | 分级设色图全景 + 左下角图例 |
| 7 | (2) GeoJSON | 比例符号图 + 右下角图例 |
| 8 | (2) GeoJSON | 鼠标悬停高亮效果 |
| 9 | (2) GeoJSON | 点要素过滤展示（importance>=4） |
| 10 | (3) MapLibre | Liberty 底图 + 聚合圆 + 专题面 + 路线 |
| 11 | (3) MapLibre | Dark 深色底图效果 |
| 12 | (3) MapLibre | 暖色主题/对比主题切换效果 |
| 13 | (3) MapLibre | 点击聚合圆展开效果 |
| 14 | (3) MapLibre | 3D 建筑拉伸（zoom >= 10, pitch 50） |
| 15 | (4) P1 | 综合地图全景 + 图层控件展开 |
| 16 | (4) P1 | 信息面板联动（点击省份） |
| 17 | (6) 双变量 | 3×3 矩阵 + 行政区双变量配色 |
| 18 | (7) 编辑器 | 添加标注点 + 编辑面板展示 |
| 19 | (5) 部署 | 部署说明页面（本地运行 + GitHub Pages） |
| 20 | 线上 | GitHub Pages 线上运行效果（部署后补充） |

---

## 项目文件清单

| 文件 | 说明 |
|------|------|
| `index.html` | 实验主页面，7 个 Tab 面板 |
| `app.js` | 全部地图逻辑，约 800 行 |
| `styles.css` | 页面样式，含双变量矩阵和编辑器样式 |
| `data/china-provinces.geojson` | 中国省级行政区 GeoJSON 数据 |
| `tileserver-config.json` | tileserver-gl-light 配置文件 |
| `styles/china-style.json` | 本地 PBF 矢量瓦片 Style JSON |
| `start.bat` | Windows 一键启动脚本 |
